function matrix(num) {
    let buff = "";
    for (let i = 0; i < num; i++) {
        let secondBuff = "";
        buff = num 
        for(let j = 0 ; j < buff  ; j++){
            secondBuff += num + " ";
        }
        console.log(secondBuff);
    }

}
matrix(3);
matrix(7);
matrix(2);
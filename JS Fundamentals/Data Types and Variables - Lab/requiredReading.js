function requiredReading(bookSize, pagesPerHour, days){

    let totalTime = bookSize / pagesPerHour;
    let hoursPerDay = totalTime / days;
    console.log(hoursPerDay);

}
requiredReading(212,20 ,2 )
requiredReading(432,15 ,4 )